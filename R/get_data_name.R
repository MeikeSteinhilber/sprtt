get_data_name <- function(input1, y, env) {
  # input1_name <- deparse(substitute(input1))
  # if(!is.null(y)){
  #   y_name <- deparse(substitute(y))
  #   paste(input1_name, "and ", y_name)
  # } else{
  #   paste(input1_name)
  # }
}
