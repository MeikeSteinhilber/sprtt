# Clear cached simulation data

**\[experimental\]** Removes locally cached simulation data. Data will
be re-downloaded on next use.

## Usage

``` r
cache_clear()
```

## Examples

``` r
if (FALSE) { # \dontrun{
# Clear cache
cache_clear()
} # }
```
